# This file makes 'press_release_app' a Python package.
# It can be empty.
# It can also be used for package-level initializations,
# for example, creating the Flask app instance if following certain patterns,
# but for now, keeping it simple for package discovery.

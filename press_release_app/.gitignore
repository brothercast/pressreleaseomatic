# Environment variables
.env

# Virtual environment
venv/
*.venv

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# IDE and editor specific files
.idea/
.vscode/
*.swp
*.swo

# SQLite database file (if you decide not to commit it, common for dev dbs)
# press_release.db 
# For now, let's keep press_release.db out of .gitignore to allow the initial schema to be part of the repo if desired.
# If it grows large or contains sensitive test data, it should be ignored.

# Build artifacts
dist/
build/
*.egg-info/

# Pytest cache
.pytest_cache/
